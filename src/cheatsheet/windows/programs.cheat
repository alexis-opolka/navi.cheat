% windows, explorer
$ files: ls | Format-List -Property Name | jq --raw-input "select(. != \"\") | gsub(\"Name : \"; \"\")"

# Windows file explorer
explorer <files>

# Windows file explorer with pre-loaded files
explorer <files>

# A simple echo but as a PowerShell command
Write-Output <hello_world>
